meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{url}}/api/login
  body: json
  auth: none
}

body:json {
  {
    "user": "{{user}}",
    "password": "{{password}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
